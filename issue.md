1. Dashboard:
 - Chế độ xem lưới: Công việc sắp tới, chỉ hiện thị task tương ứng với Trạng thái của project. 
 - Chế độ xem lịch: cần làm lại UI của các task ở trong từng ô ngày. hiện tại hơi khó nhìn, UI của từng ô cũng không đồng bộ style với app hiện tại
 - Cạnh text tiêu đề "Dự án đang triển khai" trong chế độ lưới, bổ xung toggle Sort để chọn cách sắp xếp các dự án.
 - Nút bộ lọc để bên cạnh nút sort, hiển thị trong cả 2 mode lưới và lịch. 
 - dialog của Bộ lọc: opacity các UI trong này 100% và phải tách biệt với nhau khi mở selection box, bo góc 50% hiện tại để không che chữ.
 - Dialog sửa task: opacity nền 100%, các UI bên trong 100% và tách biệt với nhau khi mở selection box, bo góc 50% hiện tại để không che chữ. ô select người thực hiện bị hiện thị ID chứ k hiện tên, cần fix lại. Giai đoạn/trạng thái/độ ưu tiên cần hiển thị chọn màu rõ ràng hơn.
 - Dialog xem list task của ngày trong lịch: Định dạng chữ các thành phần phải chuẩn style app, hiện đang bị lệch style.
 2. Projects:
 - Bổ xung sort/filter/search/page pagination cho trang xem list project theo Giai đoạn.